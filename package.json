{
  "name": "coopconnect",
  "version": "1.0.0",
  "description": "Co-opConnect — Cooperative Gig Services Platform for Household & Community Services",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client && npm install concurrently --no-save",
    "dev": "npx concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "seed": "npm run seed --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "dependencies": {
    "openai": "^7.16.0",
    "vite": "^8.3.0"
  }
}
